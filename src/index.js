import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App'; // Use uppercase A to match your file

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);